import { Sandpack } from "@codesandbox/sandpack-react";

# 5. JS sandbox

## Sandpack - ReactJS basic

<Sandpack template="react" />

## Sandpack - ReactJS complete
Dark theme, dependencies, multiple files.
<Sandpack
  theme="dark"
  customSetup={{
    entry: "/App.js",
    dependencies: { 
      "react": "latest",
      "react-dom": "latest",
      "react-markdown": "latest" 
    },
  }}
  files={{
    "/App.jsx": `import ReactDOM from 'react-dom';
import ReactMarkdown from 'react-markdown';

export default function App() {
  return <ReactMarkdown># Hello, *world*!</ReactMarkdown>;
}

const root = ReactDOM.createRoot(
    document.getElementById('root')
);
root.render(<App/>);`,
    "/index.html": `<html>
    <body>
        <div id="root"></div>
    </body>
</html>`,
}}
/>

## Sandpack - VueJS basic

<Sandpack template="vue" />

## Docusaurus (native)

```jsx live
function Clock(props) {
  const [date, setDate] = useState(new Date());

  useEffect(() => {
    const timerID = setInterval(() => tick(), 1000);
    return () => clearInterval(timerID);
  });

  function tick() {
    setDate(new Date());
  }

  return (
    <div>
      <h2>It is {date.toLocaleTimeString()}.</h2>
    </div>
  );
}
```
